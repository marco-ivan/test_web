<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <!-- Bootstrap CSS -->
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
        <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
        <link rel="stylesheet" href="/style/Main.css">
        <title>Mix n Match</title>
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="/">Mix & Match</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
          
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                  <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="/about_us">About Us</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="/forum">Forum</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="/guide">Guide</a>
                </li>
              </ul>
              <ul class="nav navbar-nav">
                <% if(!currentUser){ %>
                <li class="mr-3"><a href="/register" class="text-light display-5">Signup  <i class="fa fa-user-plus"></i></a></li>
                <li><a href="/login" class="text-light display-5">Login  <i class="fa fa-user"></i></a></li>
                <% } else { %>
                <li class="mr-3"><a href="/profile" class="text-light display-5">Welcome, <%= currentUser.Username %></a></li>
                <li><a href="/logout" class="text-light display-5">Logout <i class="fa fa-user"></i></a></li>
                <% } %>
              </ul>
            </div>
          </nav>
            <% if(error && error.length > 0){ %>
              <div class="container mt-3 mb-0 p-0">
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
              </div>
            <% } %>
            <% if(success && success.length > 0){ %>
              <div class="container mt-3 mb-0 p-0">
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
              </div>
            <% } %>